# 🚀 GitHub Release & Commit 监控机器人

一个轻量级的 Telegram 机器人，用于监控 GitHub 仓库的 Release 发布和 Commit 提交。

## ✨ 特性

- 📦 **监控 Release** - 新版本发布时即时通知
- 📝 **监控 Commit** - 跟踪指定分支的最新提交
- 🔔 **灵活通知** - 支持私聊或频道通知
- ⚡ **轻量高效** - 只有 ~1000 行代码
- 🐳 **容器化部署** - Docker/Docker Compose 一键部署
- 🔒 **权限控制** - 仅授权管理员可操作

## 🛠️ 快速开始

### 1. 配置环境变量

编辑 `.env` 文件：

```bash
# Telegram Bot Token（必需）
TELEGRAM_BOT_TOKEN=your_bot_token_here

# 管理员 User ID（必需）
ADMIN_ID=your_user_id_here

# GitHub Token（可选，提高 API 限制）
GITHUB_TOKEN=ghp_your_github_token
```

### 2. 部署

**使用 Docker Compose（推荐）：**

```bash
docker-compose up -d
```

**使用安装脚本：**

```bash
sudo ./install.sh
```

## 📖 使用方法

### 查看帮助

```
/start 或 /help
```

### 查看已监控的仓库

```
/list
```

### 添加仓库监控

**基本用法：**
```
/add owner/repo
```
默认监控 Release 和 Commit，分支为 main，通知到私聊。

**指定监控类型：**
```
/add aiogram/aiogram --release           # 只监控 Release
/add owner/repo --commit                 # 只监控 Commit
/add owner/repo -r -c                    # 监控两者（简写）
```

**指定分支：**
```
/add owner/repo --branch master
/add owner/repo -b develop
```

**发送到频道：**
```
/add owner/repo @yourchannel
```
⚠️ **注意**：需要先将机器人添加为频道管理员

**完整示例：**
```
/add kubernetes/kubernetes --release --branch main @k8s_updates
```

### 删除监控

先查看序号：
```
/list
```

然后删除：
```
/delete 1
```

## 📊 优势对比

| 特性 | 本机器人 | GitHub Notifications |
|------|---------|---------------------|
| 自定义监控 | ✅ | ❌ |
| 频道推送 | ✅ | ❌ |
| 监控多仓库 | ✅ | ✅ |
| Commit 监控 | ✅ | ❌ |
| 私有部署 | ✅ | ❌ |

## 🔧 配置说明

### GitHub Token

| 方式 | API 限制 | 适用场景 |
|------|----------|----------|
| 不使用 Token | 60 次/小时 | 监控 1-2 个仓库 |
| 使用 Token | 5000 次/小时 | 监控多个仓库 |

**获取 Token：**
1. 访问 https://github.com/settings/tokens
2. Generate new token (classic)
3. 勾选 `public_repo` 权限
4. 复制并配置到 `.env`

## 📝 日志查看

```bash
# 实时查看日志
docker-compose logs -f

# 查看最近 100 行
docker-compose logs --tail=100
```

## 🏗️ 项目结构

```
cmd/bot/
├── main.go          # 程序入口 (76 行)
├── handlers.go      # 命令处理 (257 行)
├── github.go        # GitHub API (270 行)
├── telegram.go      # Telegram API (218 行)
├── config.go        # 配置管理 (69 行)
├── constants.go     # 常量定义 (70 行)
└── utils.go         # 工具函数 (96 行)

总计：~1000 行代码
```

## 🔒 安全说明

- 仅授权管理员可以操作
- 未授权访问会被记录日志
- 支持私有仓库（配置 GitHub Token 时选择 `repo` 权限）

## 📄 许可证

MIT License

## 🙋 常见问题

**Q: 如何获取管理员 User ID？**  
A: 发送消息给 @userinfobot

**Q: 频道推送失败？**  
A: 确保机器人是频道管理员且有"发布消息"权限

**Q: API 限制不够用？**  
A: 配置 GitHub Token 提升到 5000 次/小时

**Q: 如何修改检查频率？**  
A: 编辑 `cmd/bot/constants.go` 中的 `checkInterval`

---

**版本**: v2.0 (简化版)  
**更新时间**: 2026-01-28
